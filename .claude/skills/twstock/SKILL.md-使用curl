---
name: twstock
description: 查詢台灣股市即時股價。使用者輸入股票代號（如 2330 或 2330,2317,2454），取得當前股價、漲跌幅等資訊。支援逗號分隔一次查詢多支股票。當使用者詢問台灣股票價格、報價或提及台灣股票代號時觸發。
---

# 台灣股市即時股價查詢

查詢台灣證券交易所（TWSE）及櫃買中心（OTC）的即時股價資訊。

## 何時使用此技能

當使用者：

- 詢問台灣股票的即時股價（例如「2330 現在多少錢」）
- 輸入台灣股票代號查詢行情
- 提到「查股價」、「股票」、「台股」或輸入台灣股票代號
- 詢問台灣股市價格

## 執行步驟

### 步驟一：取得股票代號

若使用者未提供股票代號，請詢問：

> 請輸入要查詢的台灣股票代號（例如：2330，或用逗號分隔多支：2330,2317,2454）

使用者可用逗號分隔多個股票代號（例如 `2330,2317,2454`）。解析輸入時以逗號分割並去除空白。

### 步驟二：取得即時股價資料

使用證交所 MIS API 取得即時報價。API 支援以 `|` 分隔的批次查詢。

**多支股票查詢時，以 `|` 串接所有代號：**

先以上市（TWSE）方式查詢所有代號：

```bash
curl -s "https://mis.twse.com.tw/stock/api/getStockInfo.jsp?ex_ch=tse_{代號1}.tw|tse_{代號2}.tw|tse_{代號3}.tw"
```

API 會在 `msgArray` 中回傳所有查到的股票資料。若有代號未出現在回應中（即 `msgArray` 中找不到），則以上櫃（OTC）方式重新查詢該代號：

```bash
curl -s "https://mis.twse.com.tw/stock/api/getStockInfo.jsp?ex_ch=otc_{未找到的代號}.tw"
```

### 步驟三：解析 JSON 回應

API 回傳的 JSON 包含 `msgArray` 欄位。各項目的關鍵欄位如下：

| 欄位 | 說明 |
|------|------|
| `c`  | 股票代號 |
| `n`  | 股票名稱 |
| `z`  | 當盤成交價（即時股價）|
| `y`  | 昨收價 |
| `o`  | 開盤價 |
| `h`  | 最高價 |
| `l`  | 最低價 |
| `v`  | 累積成交量（張）|
| `t`  | 最近成交時刻 |
| `d`  | 日期 |

若 `z` 為 `"-"`，表示該股今日尚未成交；顯示 `y`（昨收價）並註明目前可能非開盤時間。

### 步驟四：計算漲跌並顯示

計算：
- **漲跌**：`z - y`（即時股價減昨收價）
- **漲跌幅**：`((z - y) / y) * 100`%

### 步驟五：向使用者呈現結果

**單支股票** — 以詳細表格顯示：

```
## {股票名稱} ({股票代號})

| 項目       | 數值        |
|-----------|------------|
| 即時股價    | {z}        |
| 漲跌       | {change} ({change_pct}%) |
| 開盤價     | {o}        |
| 最高價     | {h}        |
| 最低價     | {l}        |
| 昨收價     | {y}        |
| 成交量(張) | {v}        |
| 更新時間   | {d} {t}    |
```

**多支股票** — 以比較摘要表格顯示，每支股票一列：

```
| 股票       | 即時股價  | 漲跌          | 開盤    | 最高    | 最低    | 昨收    | 成交量(張) |
|-----------|---------|--------------|--------|--------|--------|--------|-----------|
| {n} ({c}) | {z}     | {change} ({change_pct}%) | {o} | {h} | {l} | {y} | {v} |
```

資料更新時間統一顯示於表格下方。

漲跌指標：
- 上漲：前綴 ▲
- 下跌：前綴 ▼
- 平盤：顯示「平盤」

## 錯誤處理

- 若 `tse` 和 `otc` 的 `msgArray` 皆為空或不存在，告知使用者該股票代號可能無效。
- 若 API 無法連線，告知使用者證交所 API 可能暫時無法使用（通常於交易時段可用：週一至週五 09:00-13:30 台灣時間）。
- 若 `z` 為 `"-"`，顯示昨收價並註明目前可能非開盤時間或該股尚未成交。

## 使用範例

**查詢單支股票：**

**使用者：** /twstock 2330

**執行方式：**
1. 執行 `curl -s "https://mis.twse.com.tw/stock/api/getStockInfo.jsp?ex_ch=tse_2330.tw"`
2. 解析 JSON 回應
3. 計算與昨日的漲跌
4. 顯示詳細資料表格

**查詢多支股票：**

**使用者：** /twstock 2330,2317,2454

**執行方式：**
1. 執行 `curl -s "https://mis.twse.com.tw/stock/api/getStockInfo.jsp?ex_ch=tse_2330.tw|tse_2317.tw|tse_2454.tw"`
2. 檢查哪些代號未出現在回應中，將未找到的代號以 `otc` 前綴重新查詢
3. 解析所有結果，計算漲跌
4. 顯示比較摘要表格

## 注意事項

- 使用 Bash 的 `curl` 直接呼叫 API，回應為 JSON 格式，如有需要可用 `python3 -c` 解析。
- 證交所 API 在台灣交易時段最為穩定（週一至週五 09:00-13:30，UTC+8）。
- 支援以逗號分隔多個代號進行批次查詢（例如 `tse_2330.tw|tse_2317.tw`）。
- 一律先嘗試 `tse`（上市）；若 `msgArray` 為空，再以 `otc`（上櫃）重試。
